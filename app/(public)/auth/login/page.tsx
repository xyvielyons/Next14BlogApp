import type {Metadata} from 'next'
export const metadata: Metadata = {
  title: "Login",
  description: "Login to your blogging account",
};

function Login() {
  return (
    <div>Login</div>
  )
}

export default Login